import * as React from 'react';
import Header from '@/common/uikit/header';
import i18n from '@/common/utils/i18n';


const Survey = () => {
    return (
        <Header title={i18n.SURVEY_TITLE}/>
    );
}

export default Survey;